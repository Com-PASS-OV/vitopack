% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columns.R
\name{create_policy_exposure_columns_m}
\alias{create_policy_exposure_columns_m}
\title{Build per-period exposure columns (in years)}
\usage{
create_policy_exposure_columns_m(
  data,
  exp_names,
  start_months,
  end_months,
  start_policy_var,
  end_policy_var
)
}
\arguments{
\item{data}{A data.frame or data.table.}

\item{exp_names}{Character vector with the names of the new exposure
columns.}

\item{start_months, end_months}{Character vectors of period boundaries that
can be coerced via \code{\link[base:as.Date]{base::as.Date()}}. Same length as \code{exp_names}.}

\item{start_policy_var, end_policy_var}{Names of the columns holding the
policy start and end dates.}
}
\value{
The updated data.table (modified in place).
}
\description{
For each requested exposure period \verb{[start_months[i], end_months[i]]}
the function adds a column \code{exp_names[i]} to \code{data} containing the
overlap of that period with each row's policy span
\verb{[start_policy_var, end_policy_var]}, expressed in \strong{years}
(days / 365).
}
\examples{
dt <- data.frame(
  policy_start = as.Date(c("2024-01-15", "2024-06-01")),
  policy_end   = as.Date(c("2024-12-31", "2025-05-31"))
)
create_policy_exposure_columns_m(
  data            = dt,
  exp_names       = c("exp_2024_Q1", "exp_2024_Q2"),
  start_months    = c("2024-01-01", "2024-04-01"),
  end_months      = c("2024-03-31", "2024-06-30"),
  start_policy_var = "policy_start",
  end_policy_var   = "policy_end"
)
}
