% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columns.R
\name{create_policy_exposure_days_columns}
\alias{create_policy_exposure_days_columns}
\title{Build per-period exposure columns (in days)}
\usage{
create_policy_exposure_days_columns(
  data,
  exp_names,
  start_months,
  end_months,
  start_policy_var,
  end_policy_var
)
}
\arguments{
\item{data}{A data.frame or data.table.}

\item{exp_names}{Character vector with the names of the new exposure
columns.}

\item{start_months, end_months}{Character vectors of period boundaries that
can be coerced via \code{\link[base:as.Date]{base::as.Date()}}. Same length as \code{exp_names}.}

\item{start_policy_var, end_policy_var}{Names of the columns holding the
policy start and end dates.}
}
\value{
The updated data.table (modified in place).
}
\description{
Sister of \code{\link[=create_policy_exposure_columns_m]{create_policy_exposure_columns_m()}} — same logic, but the
exposure is expressed in \strong{days} (rounded) instead of years.
}
\examples{
dt <- data.frame(
  policy_start = as.Date("2024-01-15"),
  policy_end   = as.Date("2024-12-31")
)
create_policy_exposure_days_columns(
  data            = dt,
  exp_names       = "exp_days_q1",
  start_months    = "2024-01-01",
  end_months      = "2024-03-31",
  start_policy_var = "policy_start",
  end_policy_var   = "policy_end"
)
}
