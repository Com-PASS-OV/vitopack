% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columns.R
\name{create_find_columns}
\alias{create_find_columns}
\title{Add zero/value columns based on equality conditions}
\usage{
create_find_columns(
  data,
  condition_names,
  new_cols_names,
  condition_var,
  values_var
)
}
\arguments{
\item{data}{A data.frame or data.table.}

\item{condition_names}{Character vector of values to match against
\code{condition_var}.}

\item{new_cols_names}{Character vector with the names of the new columns to
create. Same length as \code{condition_names}.}

\item{condition_var}{Character scalar - name of the column to test.}

\item{values_var}{Character scalar - expression that yields the value to
assign where the condition matches.}
}
\value{
The updated data.table (modified in place).
}
\description{
For each pair \verb{(condition_names[i], new_cols_names[i])} the function adds a
new column to \code{data} initialised to \code{0}. Where the row matches
\code{data[[condition_var]] == condition_names[i]}, the new column is set to the
value of \code{data[[values_var]]} (evaluated as an expression - see Details).
}
\details{
The \code{values_var} argument is parsed and evaluated inside \code{data[, ...]},
which means it can be a bare column name or a small data.table-style
expression. The function modifies \code{data} by reference (via
\code{\link[data.table:setDT]{data.table::setDT()}}) and also returns it invisibly to allow chaining.
}
\examples{
dt <- data.frame(line = c("MTPL", "CASCO", "MTPL"), premium = c(100, 200, 300))
create_find_columns(
  data = dt,
  condition_names = c("MTPL", "CASCO"),
  new_cols_names = c("MTPL_premium", "CASCO_premium"),
  condition_var = "line",
  values_var = "premium"
)
}
