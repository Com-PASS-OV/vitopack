% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chain-ladder.R
\name{create_chl_coefs_weighted}
\alias{create_chl_coefs_weighted}
\title{Volume-weighted CHL factors with an explicit weight triangle}
\usage{
create_chl_coefs_weighted(cum_trg, trg_weight, chl_length = "full")
}
\arguments{
\item{cum_trg}{Cumulative triangle.}

\item{trg_weight}{Weight matrix of the same shape as \code{cum_trg}.}

\item{chl_length}{As in \code{\link[=create_chl_coefs]{create_chl_coefs()}}.}
}
\value{
A \code{data.frame} analogous to the output of \code{\link[=create_chl_coefs]{create_chl_coefs()}}.
}
\description{
Same idea as \code{\link[=create_chl_coefs]{create_chl_coefs()}} but every cell is weighted by an
arbitrary \code{trg_weight} matrix evaluated at the \strong{previous} column
(\code{j - 1}). Useful when the natural exposure differs from the cumulative
triangle itself (e.g. paid claims weighted by exposure-as-of-prior-period).
}
