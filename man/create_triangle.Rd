% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangles.R
\name{create_triangle}
\alias{create_triangle}
\title{Build a development triangle from row-level claims data}
\usage{
create_triangle(
  data,
  row_num,
  col_num,
  value,
  cond_variable = NULL,
  cond_value = NULL,
  rows = NULL
)
}
\arguments{
\item{data}{A data frame (or tibble) with at least the columns named by
\code{row_num}, \code{col_num} and \code{value}.}

\item{row_num}{Character — name of the column with row indices (origin
periods, 1-based).}

\item{col_num}{Character — name of the column with column indices
(development periods, 0-based).}

\item{value}{Character — name of the numeric column to be summed.}

\item{cond_variable}{Optional character vector — names of columns to
filter on.}

\item{cond_value}{Optional vector of values, same length as
\code{cond_variable} — equality filter applied pairwise.}

\item{rows}{Optional integer — size of the output triangle. Defaults to
the maximum value of \code{data[[row_num]]}.}
}
\value{
A numeric \verb{rows x rows} matrix.
}
\description{
Aggregates \code{value} over \verb{(row_num, col_num)} (optionally after filtering
by one or more \code{cond_variable == cond_value} pairs) and arranges the
result into a square \verb{rows x rows} matrix where the upper-right corner
beyond the main anti-diagonal is \code{NA}.
}
\examples{
df <- expand.grid(origin = 1:3, dev = 0:2)
df$paid <- c(100, 50, 20,  120, 40, NA,  130, NA, NA)
df <- df[!is.na(df$paid), ]
create_triangle(df, row_num = "origin", col_num = "dev", value = "paid")
}
