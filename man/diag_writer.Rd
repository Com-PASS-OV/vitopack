% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangles.R
\name{diag_writer}
\alias{diag_writer}
\title{Append the latest diagonal of a new triangle to an old one}
\usage{
diag_writer(old_trg, new_trg)
}
\arguments{
\item{old_trg}{A \verb{(n) x (n)} matrix — the old triangle.}

\item{new_trg}{A \verb{(n+1) x (n+1)} matrix — the new triangle including the
freshly observed diagonal.}
}
\value{
The merged \verb{(n+1) x (n+1)} matrix.
}
\description{
Used in rolling re-runs: pads \code{old_trg} with one extra row and column of
\code{NA}, then fills in the gaps from \code{new_trg} (which already contains the
newly observed diagonal).
}
\examples{
old <- matrix(c(10, 5, 20, NA), nrow = 2, byrow = TRUE)
new <- matrix(c(10, 5, 3, 20, 6, NA, 25, NA, NA), nrow = 3, byrow = TRUE)
diag_writer(old, new)
}
