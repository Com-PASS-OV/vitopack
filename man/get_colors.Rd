% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colors.R
\name{get_colors}
\alias{get_colors}
\alias{get_colors_plus}
\alias{get_colors_duo}
\title{Pick \code{n} evenly spaced colors from a rainbow data frame}
\usage{
get_colors(rgb_df, n_colors)

get_colors_plus(rgb_df, n_colors)

get_colors_duo(rgb_df, n_colors)
}
\arguments{
\item{rgb_df}{A data frame with columns \code{r}, \code{g}, \code{b}, e.g. produced by
\code{\link[=rgb_colors_for_plot]{rgb_colors_for_plot()}}.}

\item{n_colors}{Integer >= 2 -- number of colors to return.}
}
\value{
A character vector of length \code{n_colors} with hex color codes
(\code{"#RRGGBB"}).
}
\description{
Three flavours are exported. They differ only in the post-processing they
apply after picking equally-spaced rows from \code{rgb_df}:
}
\details{
\itemize{
\item \code{get_colors()} -- no post-processing.
\item \code{get_colors_plus()} -- every other color is darkened (x0.75) or
lightened (towards 255 with weight 0.7), creating a strong duo pattern.
\item \code{get_colors_duo()} -- every other color is darkened (x0.8) or lightened
(weight 0.6), milder than the \verb{_plus} variant.
}
}
\examples{
rgb_df <- rgb_colors_for_plot()
get_colors(rgb_df, 5)
get_colors_plus(rgb_df, 6)
get_colors_duo(rgb_df, 6)
}
